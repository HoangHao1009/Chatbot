from setuptools import setup

setup(
  name = 'chatbot',
  version = '0.1',
  author = 'Ha Hoang Hao',
  packages = ['chatbot'],
  description = 'seq2seq architecture chatbot',
  install_requires = ['torch'],
)
